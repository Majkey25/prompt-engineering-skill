interface:
  display_name: "Prompt Engineering"
  short_description: "Build source-backed, testable prompt contracts"
  default_prompt: "Use $prompt-engineering to turn this rough task into a verified prompt contract."
policy:
  allow_implicit_invocation: true
